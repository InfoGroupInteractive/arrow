import * as colors from './theme/colors';
import * as reportTheme from './theme/report-theme';
import * as constants from './theme/theme.constants';
import * as theme from './theme/theme';

export const {
    theme: {
        colors,
        reportTheme,
        constants,
        theme
    }
};